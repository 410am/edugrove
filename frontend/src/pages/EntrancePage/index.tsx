import SignIn from "../components/SignIn";
import MakeRoom from "./MakeRoom";

const EntrancePage = () => {
  // 1. 로그인
  // 2. 회의 참가
  // 3. 회의 만들기
  // 2,3번 묶으면 될듯

  return (
    <div>
      <MakeRoom />
    </div>
  );
};

export default EntrancePage;
